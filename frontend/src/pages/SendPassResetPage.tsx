import ResetPassResetMail from "../components/SendPassResetMail";

export default function SendPassResetPage() {
  return (
    <div className="min-h-screen flex items-center justify-center">
      <ResetPassResetMail />
    </div>
  );
}
